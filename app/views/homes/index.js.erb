// console.log(event.target.text)
// console.log(event.target.tagName)
// console.log(window.event)
const printID = (e) => {
  e = e || window.event;
  e = e.target || e.srcElement;
  console.log("ID: " + e.id);
  if (document.getElementById(e.id).text == 'like') {
    document.getElementById(e.id).text = 'dislike';
  } else {
    document.getElementById(e.id).text = 'like';
  }
  document.getElementById(e.id).classList.toggle('btn-warning');
  document.getElementById(e.id).classList.toggle('btn-success');
}
// button.value = 'dislike'
const loadIndex = () => {
  console.log('Hello from homes#index.js');
}
